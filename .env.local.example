# Optional — the app works fully without these (local-only progress, no sync).
# See README.md "Cross-device sync" section for setup steps.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional — improves social share image resolution. Auto-inferred on Vercel if unset.
NEXT_PUBLIC_SITE_URL=
